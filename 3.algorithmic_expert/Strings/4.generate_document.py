characters = "Bste!hetsi ogEAxpelrt x "
document = "AlgoExpert is the Best!"

# n - len of char in string
# m - len of doc in string
def generateDocument(characters, document):
    
    print(characters)
    # document.sort()
    # print(document)
	# if sorted(characters) == sorted(document) and sorted(characters) > sorted(document):
	# 	return True
	# else:
	# 	return False

print(generateDocument(characters, document))